apiVersion: apps/v1
kind: Deployment
metadata:
  name: opentoggl
  namespace: opentoggl-local
spec:
  replicas: 1
  selector:
    matchLabels:
      app: opentoggl
  template:
    metadata:
      labels:
        app: opentoggl
    spec:
      containers:
        - name: opentoggl
          image: ghcr.io/correctroadh/opentoggl:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: OPENTOGGL_SERVICE_NAME
              value: opentoggl
            - name: PORT
              value: "8080"
            - name: OPENTOGGL_FILESTORE_NAMESPACE
              value: opentoggl
            - name: OPENTOGGL_JOBS_QUEUE_NAME
              value: default
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: opentoggl-secrets
                  key: postgres-password
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: opentoggl-secrets
                  key: redis-password
            - name: DATABASE_URL
              value: postgres://postgres:$(POSTGRES_PASSWORD)@$(POSTGRES_SERVICE_HOST):$(POSTGRES_SERVICE_PORT_POSTGRES)/opentoggl?sslmode=disable
            - name: REDIS_URL
              value: redis://:$(REDIS_PASSWORD)@$(REDIS_SERVICE_HOST):$(REDIS_SERVICE_PORT_REDIS)/0
          ports:
            - containerPort: 8080
              name: http
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: opentoggl
  namespace: opentoggl-local
spec:
  type: NodePort
  selector:
    app: opentoggl
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
